version: "3.9"

services:
  parser:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./logs:/app/logs
